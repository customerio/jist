org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Committed source of truth for the Android package version. Bumped by the
# "Release Android" workflow, which tags android-v<VERSION_NAME>; "Publish Android"
# derives the published version from that tag. Seeded at 0.0.0 — the first release
# is a `minor` bump to 0.1.0.
VERSION_NAME=0.1.0
