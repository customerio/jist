{
  "name": "jist-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../core/jist-core && cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir ../../web/src/wasm --out-name jist_core ../target/wasm32-unknown-unknown/release/jist_core.wasm",
    "build": "tsc && cp -r src/wasm dist/wasm",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "serve": "^14.2.4",
    "typescript": "^5.7.0"
  }
}
